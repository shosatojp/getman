import { ClickSelectDirective } from './click-select.directive';

describe('ClickSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickSelectDirective();
    expect(directive).toBeTruthy();
  });
});
